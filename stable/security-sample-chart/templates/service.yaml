apiVersion: v1
kind: Service
metadata:
  name: {{ include "security-sample-chart.fullname" . }}
  labels:
    {{- include "security-sample-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "security-sample-chart.selectorLabels" . | nindent 4 }}
